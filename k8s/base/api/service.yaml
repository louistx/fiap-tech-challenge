apiVersion: v1
kind: Service
metadata:
  name: fiap-tech-challenge-api-service
spec:
  type: ClusterIP
  selector:
    app: fiap-tech-challenge-api
  ports:
    - name: http
      port: 8080
      targetPort: http
