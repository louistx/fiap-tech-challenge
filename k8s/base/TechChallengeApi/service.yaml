apiVersion: v1
kind: Service
metadata:
  name: fiap-tech-challenge-api-service
  labels:
    app: fiap-tech-challenge-api
spec:
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: fiap-tech-challenge-api
  type: NodePort