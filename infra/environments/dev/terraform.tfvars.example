# Copie para "terraform.tfvars" (gitignored) apenas se quiser sobrescrever
# os defaults. Sem esse arquivo, `terraform apply` já funciona com valores
# de desenvolvimento equivalentes ao docker-compose.

# cluster_name       = "fiap-tech-challenge"
# worker_count       = 2
# host_http_port     = 8080
# postgres_user      = "postgres"
# postgres_password  = "Dev@123456"
# postgres_db        = "TechChallenge"
